import 'package:flutter/material.dart';

class AnimalListItemIconView extends StatelessWidget {
  const AnimalListItemIconView({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.abc);
  }
} 